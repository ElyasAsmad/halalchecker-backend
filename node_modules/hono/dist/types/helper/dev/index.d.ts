import type { Hono } from '../../hono';
interface ShowRoutesOptions {
    verbose?: boolean;
}
interface RouteData {
    path: string;
    method: string;
    name: string;
    isMiddleware: boolean;
}
export declare const inspectRoutes: (hono: Hono) => RouteData[];
export declare const showRoutes: (hono: Hono, opts?: ShowRoutesOptions) => void;
export {};
